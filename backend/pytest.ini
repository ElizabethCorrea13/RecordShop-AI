[pytest]
; Agrega backend/ (donde vive este archivo) al sys.path, así los tests
; pueden hacer "import main", "import catalog", etc. sin instalar el
; proyecto como paquete.
pythonpath = .
testpaths = tests
